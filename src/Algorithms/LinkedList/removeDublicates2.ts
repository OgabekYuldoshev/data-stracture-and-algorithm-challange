import { ListNode } from "@/DataStructures/LinkedList/ListNode";

export const removeDublicates2 = (head: ListNode | null): ListNode | null => {
  if (!head) {
    return head;
  }

  return head;
};
